# ============================================================
# campusCar 机器人配置文件
# 换车时只需修改这一个文件
# ============================================================

# ── 底盘网络 ──────────────────────────────────────────────
CAR_IP="192.168.100.2"
CAR_USER="bingda"
CAR_PASS="bingda"
SUDO_PASS="1"

# ── ROS2 话题 ─────────────────────────────────────────────
CMD_VEL_TOPIC="/cmd_vel"
FIX_TOPIC="/fix"
HEADING_TOPIC="/heading"
IMAGE_TOPIC="/camera/color/image_flipped"
IMAGE_TOPIC_ALT="/usb_cam/image_raw"

# ── 速度限制 ──────────────────────────────────────────────
MAX_LINEAR_SPEED=1.0      # m/s
MAX_ANGULAR_SPEED=1.0     # rad/s
DEFAULT_LINEAR_SPEED=0.3  # m/s（GUI 默认值）
DEFAULT_ANGULAR_SPEED=0.5 # rad/s（GUI 默认值）

# ── 底盘启动命令（远程 SSH）────────────────────────────────
# 不同底盘修改这里
CAR_LAUNCH_CMD="source ~/ros2_ws/install/setup.bash && ros2 launch base_control_ros2 base_control.launch.py"

# ── 串口 / RTK ────────────────────────────────────────────
RTK_BAUD=115200
ROSBRIDGE_PORT=9090

# ── 视频流端口 ────────────────────────────────────────────
RTSP_PORT=8554
HLS_PORT=8888
MJPEG_PORT=8080
