# 今日工作记录与明日检查清单（2026-04-30）

## 今日完成

### 1. main 分支骨架整理
- `config/robot.env` 精简为纯骨架：只含路径计算、profile 加载机制、ROS2 话题名、服务端口
- 所有车型特定内容下沉到 `config/profiles/`：
  - `old-orange-pi-orbbec.env`：旧底盘完整配置（底盘网络、速度、相机、UE 参数等）
  - `new-stm32-hikrobot.env`：新底盘完整配置（串口、Hikrobot 相机等）

### 2. hardware 分支对齐
- `hardware/old-orange-pi-orbbec` rebase 到新 main，现在继承骨架结构
- 今日现场校准值 `VEHICLE_HEADING_OFFSET_DEG=156.828` 已写入 profile

### 3. 分支清理
- 删除 `profile/old-orange-pi-orbbec`、`profile/new-stm32-hikrobot`（命名混乱，统一用 hardware/）
- 删除 `codex/hardware-adapter-refactor`（早期废弃方案，内容已被覆盖）

### 4. 目录残留清理
- `~/campusCar-old-chassis/orbbec_ws` 从软链接改为真实目录（原指向已删除的 `~/campusCar`）
- `~/campusCar` 旧 clone 已删除
- worktree git 元数据断裂已修复：重新 clone 到 `~/campusCar-git-main` 作为主仓库，`campusCar-old-chassis` 重建为其 worktree

---

## 明日检查清单

### 启动验证
```bash
cd ~/campusCar-old-chassis
./scripts/launch_all.sh
```
确认启动日志里出现：
```
Profile: old-orange-pi-orbbec (旧底盘 OrangePi + Orbbec)
底盘模式: remote_ssh
```

### 速度闭环调试
详见 `docs/速度闭环调试指南.md`，核心步骤：
1. `kill <ue_bridge PID>` 后 `python3 src/ue_bridge.py`
2. 发 Forward 指令，观察 `[速度环]` 日志
3. 调参入口：`config/profiles/old-orange-pi-orbbec.env` 里的 `UE_SPEED_KP`、`UE_SPEED_KI`

---

## 当前仓库结构

```
main                          ← 纯骨架
  config/robot.env            ← 只有话题名、端口、profile 加载
  config/profiles/
    old-orange-pi-orbbec.env  ← 旧底盘所有特定配置
    new-stm32-hikrobot.env    ← 新底盘所有特定配置

hardware/old-orange-pi-orbbec ← 旧底盘工作分支（基于 main）
hardware/new-stm32-hikrobot   ← 新底盘工作分支（待对齐 main）
```

## 本地目录结构

```
~/campusCar-git-main/         ← git 主仓库（main 分支）
~/campusCar-old-chassis/      ← worktree（hardware/old-orange-pi-orbbec）
~/campusCar-new-chassis/      ← 新底盘工程（独立仓库）
```
